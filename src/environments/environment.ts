// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyCy2JDVw2eRexyUokiJkWq_7LCcCKEgH-I',
    authDomain: 'sorena-tile.firebaseapp.com',
    databaseURL: 'https://sorena-tile-default-rtdb.firebaseio.com',
    projectId: 'sorena-tile',
    storageBucket: 'sorena-tile.appspot.com',
    messagingSenderId: '793196116684',
    appId: '1:793196116684:web:02024ecaaa308e1bf8a069',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
